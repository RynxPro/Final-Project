import React from "react";
import Navbar from "@/components/navbar/Navbar";

const page = () => {
  return (
    <section>
      <nav>
        <Navbar />
      </nav>
    </section>
  );
};

export default page;
